{
  "name": "kevinc16com",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "deploy": "yarn build && yarn assets && yarn fonts && gh-pages -d dist",
    "assets": "cp -r ./assets dist/assets",
    "fonts": "cp -r ./fonts dist/fonts"
  },
  "dependencies": {
    "bootstrap": "^5.1.3",
    "camera-controls": "^1.34.2",
    "jszip": "^3.7.1",
    "three": "^0.133.1"
  },
  "devDependencies": {
    "@types/three": "^0.133.0",
    "gh-pages": "^3.2.3",
    "gltf-pipeline": "^3.0.4",
    "sass": "^1.42.1",
    "typescript": "^4.3.2",
    "vite": "^2.6.4"
  }
}
